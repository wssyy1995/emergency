# Bug 修复历史记录

---

## Bug 1: iOS 真机打不开游戏（欢迎气泡/准备气泡渲染问题）

### 修复日期
2026-03-16 (commit: 4d15413)

### Bug 现象
- 游戏在 iOS 真机上无法正常打开或运行
- 表现为闪退、白屏或渲染崩溃
- 开发者工具正常，但真机环境崩溃

### 原因分析

#### 1. Canvas 渲染过于复杂
欢迎气泡 (`renderWelcomeBubble`) 和准备气泡 (`renderReadyBubble`) 使用了以下复杂 Canvas 操作：
- **呼吸动画缩放**: `ctx.scale(breathScale, breathScale)` - 动态变换矩阵
- **复杂贝塞尔曲线**: 绘制带小三角的气泡形状（多段 quadraticCurveTo）
- **Path2D 对象**: `new Path2D()` 创建复杂路径
- **多重阴影效果**: 大模糊半径 (`shadowBlur = 20`) 配合偏移
- **高光线条**: 额外的白色描边增加立体感

这些复杂操作在 iOS WebView/小程序环境中可能导致渲染管线崩溃。

#### 2. 位置计算 Bug（隐蔽）
```javascript
// 修复前 - 错误的坐标计算
const bubbleX = this.x/2 - 90 * this.scale  // x/2 导致位置严重偏移！

// 修复后
const bubbleX = this.x - 130 * this.scale
```

### 修复方式

#### 1. 简化气泡渲染逻辑
- ❌ 移除呼吸动画效果（ctx.translate + ctx.scale 变换）
- ❌ 移除小三角指针（复杂的贝塞尔曲线路径）
- ❌ 移除 Path2D 对象使用
- ❌ 移除内部高光线条
- ✅ 简化为标准圆角矩形

#### 2. 新增 iOS 兼容的绘制方法
```javascript
_drawRoundRect(ctx, x, y, width, height, radius) {
  ctx.beginPath()
  ctx.moveTo(x + radius, y)
  ctx.lineTo(x + width - radius, y)
  ctx.quadraticCurveTo(x + width, y, x + width, y + radius)
  // ... 简化的四段圆弧路径
  ctx.closePath()
}
```

#### 3. 降低渲染负载
| 属性 | 修复前 | 修复后 |
|------|--------|--------|
| 阴影模糊 | 20px | 8px |
| 阴影偏移 | 4px | 2px |
| 边框宽度 | 6px | 2px |
| 字体大小 | 14px | 12px |
| 气泡宽度 | 200px | 180px |

#### 4. 修复位置计算
```javascript
// 欢迎气泡
bubbleX: this.x - 130 * this.scale
bubbleY: this.y - 90 * this.scale

// 准备气泡  
bubbleX: this.x - 160 * this.scale
bubbleY: this.y - 70 * this.scale
```

### 代码变更文件
- `js/Nurse.js`: 重写 `renderWelcomeBubble()` 和 `renderReadyBubble()` 方法

### 经验总结
1. iOS 真机对 Canvas 复杂操作的支持有限，应避免动态变换矩阵和复杂路径
2. 真机测试需要尽早进行，开发者工具无法完全模拟真机环境
3. 简单的视觉降级（移除动画效果）比崩溃要好得多

---


---

## Bug 2: 【目标达成】弹窗点击【升级】按钮后无法进入升级模式

### 修复日期
2026-03-18

### Bug 现象
- 点击【目标达成】弹窗的【升级】按钮后，弹窗没有消失
- 页面卡在弹窗界面，奖杯旋转动画卡住
- 疯狂打印 "绘制关卡完成弹窗" 日志
- 升级气泡和【开始接诊】按钮没有显示

### 原因分析

#### 1. 渲染循环未正确处理升级模式
`loop()` 函数的条件判断只检查了 `isRunning` 和弹窗状态，没有检查升级模式：
```javascript
// 修复前
if (!this.isRunning && !hasModal) return

// 修复后  
if (!this.isRunning && !hasModal && !this.upgradeMode) {
  requestAnimationFrame((t) => this.loop(t))
  return
}
```

#### 2. 升级模式与弹窗状态冲突
点击【升级】按钮后，`enterUpgradeMode()` 被调用，但：
- `levelCompleteModal` 被设置为 `null`
- `upgradeMode` 被设置为 `true`
- 但渲染循环中的条件判断导致某些情况下渲染被跳过或重复执行

#### 3. 缺少升级模式的渲染保护
`renderLevelCompleteModal()` 虽然有升级模式检查，但触发时机和条件判断存在问题

### 修复方式

#### 1. 修复渲染循环条件
```javascript
loop(timestamp) {
  const hasModal = this.gameOverModal?.visible || 
                   this.levelCompleteModal?.visible || 
                   this.gameWinModal?.visible
  
  // 添加 upgradeMode 检查，确保升级模式下继续渲染
  if (!this.isRunning && !hasModal && !this.upgradeMode) {
    requestAnimationFrame((t) => this.loop(t))
    return
  }
  
  // ... 正常渲染逻辑
}
```

#### 2. 强化升级模式检查
```javascript
renderLevelCompleteModal() {
  // 【升级模式】不渲染关卡完成弹窗
  if (this.upgradeMode) {
    return  // 强制跳过
  }
  if (!this.levelCompleteModal) {
    return
  }
  // ...
}
```

#### 3. 确保升级模式状态正确设置
```javascript
enterUpgradeMode() {
  // 彻底关闭所有弹窗
  this.levelCompleteModal = null
  
  // 设置升级模式
  this.upgradeMode = true
  this.showUpgradeBubbles = true
  this.showStartButton = true
  this.isRunning = false
  
  // ...
}
```

#### 4. 多处添加保护逻辑
- `checkLevelComplete()` - 升级模式下跳过关卡完成检查
- `showLevelCompleteModal()` - 升级模式下跳过创建弹窗
- `renderLevelCompleteModal()` - 升级模式下跳过渲染

### 代码变更文件
- `js/Game.js`: 
  - 修改 `loop()` 渲染循环条件
  - 修改 `renderLevelCompleteModal()` 添加升级模式检查
  - 修改 `enterUpgradeMode()` 确保状态正确设置
  - 修改 `checkLevelComplete()` 添加升级模式保护

### 经验总结
1. 渲染循环的条件判断需要覆盖所有游戏状态（运行中、弹窗、升级模式等）
2. `requestAnimationFrame` 的递归调用需要正确处理跳过逻辑，避免中断渲染链
3. 状态变更（如进入升级模式）需要同步更新多个相关状态变量
4. 添加详细的日志有助于定位渲染时序问题

---
